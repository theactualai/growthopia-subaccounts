# Supabase (not required for the seed-data preview)
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=   # server only - never expose to the browser

# Encryption key for TOTP secrets at rest. Lives outside the database on purpose.
TOTP_ENCRYPTION_KEY=

# Anthropic - not used yet, wired in later
ANTHROPIC_API_KEY=
